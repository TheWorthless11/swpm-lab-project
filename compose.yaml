services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mydb'
      - 'POSTGRES_PASSWORD=strong_password'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
